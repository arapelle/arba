.migration conan:
  x.arba-cppx 0.1.0: vtag -> numver_constant
  x.fix all package-config. -__-
  x.(done: appt, seri, math, grid, inis, cryp.)
  .Make a release of arba-conan-index.
.arba:
  .script/
    .generate_dependency_graph.py
  .gitignore
.cmtk/0.15.0
.arba-cppx using cmtk/0.15.0
.arba-meta:
  .enum features
  .crtp features
.arba-aloc/0.1.0
.arba-exec/0.1.0
.arba:
  .script/
    .update_external_dependencies.py
    .update_conan_dependencies.py
.arba-appt/0.16.0
.arba-dnum  # more crtp xD
.temgen 0.10.0
.arba-itru:
  .isptr -> siptr
  .uiptr
  .intrusive specifically_allocated
  .silist more generic
.arba-evnt:
  .class signal using intrusive and allocator -> arba-siga ? 
.arba-rsce:
  .crtp rdec: multi, poly, vlfs, synchronized, verbose...
.arba-evnt:
  .crtp edec: multi, poly, synchronized, verbose...
.arba-appt:
  .crtp adec: multi_plugin_task
  .tout changer: refaire le crtp plus propre inspiré par arba-aloc et arba-meta.

.arba-rsce:
  .crtp (dec: logging)
.arba-vart (anciennement arba-inis)
