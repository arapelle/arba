x.migration conan:
  x.arba-cppx 0.1.0: vtag -> numver_constant
  x.fix all package-config. -__-
  x.(done: appt, seri, math, grid, inis, cryp.)
  x.Make a release of arba-conan-index.
x.arba WIP:
  x.script/
    x.generate_dependency_graph.py
  x.gitignore
x.cmtk/0.15.0
x.arba-cppx/0.2.0 using cmtk/0.15.0
x.arba-cppx/0.3.0:
  x.fix: no exceptions macros
x.arba-meta/0.4.0:
  x.enum features
  x.use arba-cppx/0.3.0
x.arba-meta/0.5.0:
  x.crtp features
x.resursive update of dependencies.
{
  x.dirn
  x.rand
  x.hash
  x.inis
  x.meta
  x.plug
  x.strn
  -
  x.core
  x.uuid
  x.math
  x.itru
  x.vlfs
  x.wgen
  -
  x.seri
  x.cryp
  x.grid
  x.stdx
  x.rsce
  x.evnt
  -
  x.appt
}
x.arba-appt/0.17.0:
  x.execution_status is a meta::enumerator.
  x.make spdlog dependency optional.
.arba-appt/0.18.0:
  .split into several targets, and make some arba dependencies optional.
.arba-itru/0.5.0
  .isptr -> siptr (..., make_shared_instrusive_ptr<T>())
  .islist -> silist
.arba-dpat/0.1.0
.Use new target name convention: arba-FEAT::COMPONENT_NAME, arba-FEAT::arba-FEAT(all components)
.arba-meta/0.6.0:
  .arithmetic & comparison helpers
.arba-aloc/0.1.0
.arba-exec/0.1.0: metronome, periodic_task, build_and_consume, request_and_build/provide/answer
.arba:
  .script/
    .update_external_dependencies.py
    .update_conan_dependencies.py
  .Make a release
.arba-dnum  # more crtp xD
.arba-appt/0.19.0
.temgen/0.10.0
.arba-itru:
  .uiptr
  .intrusive specifically_allocated
  ((.silist more generic <- whut?!?))
.arba-evnt:
  .class signal using intrusive and allocator -> arba-siga ? 
.arba-rsce:
  .crtp rdec: multi, poly, vlfs, synchronized, verbose...
.arba-evnt:
  .crtp edec: multi, poly, synchronized, verbose...
.arba-appt/0.20.0:
  .tout changer: refaire le crtp plus propre inspiré par arba-aloc et arba-meta.

.arba-rsce:
  .crtp (dec: logging)
.arba-vart (VARiableTree anciennement arba-inis)

------------------------------------------------------------------------------------------------------------------------------------------------
.Apprendre les coroutines:
https://zestedesavoir.com/articles/152/la-puissance-cachee-des-coroutines/#3-un-parseur-tres-leger-et-plutot-performant
https://github.com/lewissbaker/cppcoro

------------------------------------------------------------------------------------------------------------------------------------------------
arba-logs ? ok, mais pourquoi pas boost-logging ? et pourquoi pas spdlog ? 
arba-args ? ok, mais pourquoi pas argparse (cf. github) ? 
